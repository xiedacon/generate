# generate

### shell

```
generate init user repository license git dir

* README.md
* package.json
* LICENSE

git (flag)
  git init
  git add .
  git commit -m "init"
  git remote add origin https://github.com/{{user}}/{{repository}}.git
  git push -u origin master
```